{
  "name": "@f/deep-equal",
  "description": "Check for deep equality",
  "repository": "git://github.com/micro-js/deep-equal.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/has": "^1.0.1",
    "@f/is-array": "^1.1.1",
    "@f/is-object": "^1.1.4",
    "tape": "^4.6.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
